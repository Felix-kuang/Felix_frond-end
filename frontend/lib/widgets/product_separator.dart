import 'package:flutter/material.dart';

Widget productSeparator() => const Padding(
  padding: const EdgeInsets.symmetric(horizontal: 20),
  child: Divider(thickness: 1, color: Colors.grey, height: 0),
);
